var xml;

function preload() {
  xml = loadXML("data.xml");
}

function setup() {
  createCanvas(600, 400);
}

function draw() {
  // for (var i=0; i<lines.length; i++) {
  // 	text(lines[i], 5, 10*i);
  // }
}
